.container.content.columns
  .column.is-10.is-offset-1
    %h4 Unassigned clients

    %ul#unassigned_clients
      = render partial: 'unassigned', collection: @unassigned_clients, as: 'client'

    %h4 Assigned clients
    %ul#assigned_clients
      = render partial: 'assigned', collection: @assigned_clients, as: 'client'
