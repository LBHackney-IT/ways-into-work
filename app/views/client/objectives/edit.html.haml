.container.content.has-text-centered
  = render partial: 'shared/profile_steps'
.container.content.columns
  .column.is-10.is-offset-1
    %h2.subtitle=I18n.t('clients.steps.step_3.long')

    = simple_form_for [current_client], url: :client_objectives do |form|
      = render 'shared/form_errors', object: form.object
      .convert_checkbox
        =form.input :objectives, as: :check_boxes, collection: ObjectiveOption.all, :label => 'What would you like to discuss with your advisor?', include_hidden: false

        = render partial: 'shared/other_text', locals: { form: form, field: :other_objective }
        %p.help Choose as many options that apply to you.

      .actions
        = form.submit 'Next Step', :class => 'button is-primary is-pulled-right'
        = form.submit 'Save and Exit', :class => 'button'
