.nav-left
  = link_to I18n.t('header.title'), :hackney_works, class: 'nav-item is-brand bold'
%span.nav-toggle
  %span
  %span
  %span
.nav-right.nav-menu
  = link_to "Home", :hackney_works, class: "nav-item is-tab #{cp(:hackney_works)}"
  = link_to "Meet the team", hubs_path, class: "nav-item is-tab #{cp(hubs_path)}"
  -if cp(user_login_session_path) == "current"
    = link_to "Sign Up", new_client_path, class: "nav-item is-tab #{cp(new_client_path)}"
  -else
    = link_to "Log In", user_login_session_path, class: "nav-item is-tab #{cp(user_login_session_path)}"

