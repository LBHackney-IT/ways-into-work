- if @client.referrer
  %p
    #{@client.name} has just been referred to Hackney Works by #{@client.referrer.name} from #{OrganisationOption.display(@client.referrer.organisation)}.
- else
  %p="#{@client.name} has just registered."

%p= link_to 'View your cases here', advisor_clients_url

= render partial: 'shared/email_signature'
