- if current_advisor.team_leader
  = simple_form_for @client.object, url: advisor_client_assign_path(client_id: @client.id) do |form|
    = render 'shared/form_errors', object: form.object
    = form.input :advisor_id, as: :select, collection: current_advisor.hub.advisors, include_blank: false
    = form.submit I18n.t('clients.buttons.assign_advisor'), class: "step button is-primary"
- else
  - if @client.advisor != current_advisor
    =button_to I18n.t('clients.buttons.assign_to_me'), advisor_client_assign_path(client_id: @client.id, client: {advisor_id: current_advisor.id }), method: :put, class: "button is-primary is-small"

= @client.decorate_file_action
