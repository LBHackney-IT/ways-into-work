%section
  .head.level
    .level-left
      %h2= "Add a new External Apprenticeship"
    .level-right
      =link_to "Back to all opportunities", advisor_opportunities_path, class: 'button pull-right top_right_button'
  %hr

  .opportunity_form
    = simple_form_for @external_apprenticeship, url: advisor_external_apprenticeships_path, local: true do |f|
      = f.input :title, :input_html => { :class => 'input' }
      = f.input :end_date, as: :string, label: 'Closing date', input_html: {type: :date, class: 'input date_input', value: f.object.try(:end_date).try(:to_date)}, placeholder: "dd/mm/yyyy"
      = f.input :short_description, as: :text, label: 'Short description (max 400 characters)', input_html: { class: 'textarea', :maxlength => 400 }
      = f.input :salary, label: 'Pay', :input_html => { :class => 'input' }
      = f.input :location, label: 'Location', :input_html => { :class => 'input' }
      = f.input :long_description, as: :text, input_html: { class: 'textarea' }
      = f.submit "Add new external apprenticeship", :class => 'button is-primary tablet_cta'
