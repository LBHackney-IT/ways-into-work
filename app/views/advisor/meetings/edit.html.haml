.column
  .columns.head.level
    .level-left.column
      %h3.subtitle Meeting arrangements
    .level-right.column
      =link_to I18n.t('clients.buttons.back_to_meetings'), advisor_client_meetings_path, class: 'button pull-right'
  %hr

  = simple_form_for [client, meeting], url: advisor_client_meeting_path(client_id: client.id, id: meeting.id), method: :put do |form|

    = render partial: 'form_fields', locals: {form: form}

    = form.submit I18n.t('clients.buttons.update'), :class => 'button is-primary'