.achievement-container{ 'class' => achievements.collect(&:name).include?(option.id) ? 'acheived' : '' }
  %a.show_achievement{ 'data-target-id' => option.id }
    = option.name
    
  .is-hidden{id: "achievement_#{option.id}"}
    = render partial: 'form', locals: { option: option }
