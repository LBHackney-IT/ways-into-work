.column
  .head.level
    .level-left
      %h2="#{client.first_name}'s Achievements"

  = render partial: 'shared/upper_tabs', locals: {the_client: client}
  .profile-tab
    .column
      = render partial: 'achievement', collection: AchievementOption.all, as: :option
      = render partial: 'achievable', collection: AchievableOption.all, as: :option
          
